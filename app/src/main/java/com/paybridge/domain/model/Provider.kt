package com.paybridge.domain.model

/** The three payment apps PayBridge is allowed to read notifications from — see PaymentAppAllowlist. */
enum class Provider {
    NAYAPAY,
    EASYPAISA,
    JAZZCASH,
}
