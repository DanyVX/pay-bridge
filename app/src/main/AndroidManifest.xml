<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".PayBridgeApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          BIND_NOTIFICATION_LISTENER_SERVICE is a sensitive permission Android reserves for
          services the user explicitly enables under Settings > Notification access. It is
          NOT gated behind default-app rules the way READ_SMS now is, which is why this app
          reads payment confirmations via notifications rather than SMS. Declaring the
          permission here does not grant it — the user must flip it on manually; see
          PermissionSetupScreen for that flow.
        -->
        <service
            android:name=".data.notification.PaymentNotificationListenerService"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
